# CLAUDE.md — build-infra-study

임베디드 타깃의 **빌드·검증 인프라**를 실험으로 배우는 공개 학습 저장소.
(같은 집 스타일: `deep-learning-study`, `pose-estimation-study`)

## 이 저장소의 규칙

- **합격 기준 없는 실험은 만들지 않는다.** 실험 README는 항상
  `질문 / 방법 / 합격 기준 / 결과` 네 절을 가진다.
- **결과는 지어내지 않는다.** 실행 전이면 상태는 `미실행`이다.
  `scripts/verify.py`가 `docs/results.json`에 기록한 것만 README 표에 옮긴다.
- **측정값에는 환경을 붙인다.** 빌드 시간은 머신·코어 수·캐시 상태 없이는 의미가 없다.
- 노트는 한국어, 코드·커밋 메시지·식별자는 영어.
- 콘솔 출력(`print`, `echo`)은 ASCII만 — 개발 머신 콘솔이 cp949라 일부 문자에서 깨진다.

## 실행 환경

**리눅스 전용.** Windows에서는 WSL2 우분투 안에서, 저장소도 WSL 파일시스템
(`~/build-infra-study`)에 두고 작업한다. `/mnt/c`, `/mnt/d` 경로에 두면
파일 I/O가 느려져 빌드 시간 측정이 무의미해진다.

## 실험 추가할 때

1. `experiments/expNN_<name>/README.md` — 질문/방법/합격 기준을 **먼저** 쓴다.
2. `check.sh` — 합격 기준을 그대로 코드로 옮긴다. 성공 시 exit 0.
3. `scripts/verify.py`의 `EXPERIMENTS` 목록에 추가한다.
4. 루트 README의 실험 표에 행을 추가한다(상태는 `미실행`으로 시작).

## 맥락

임베디드 타깃의 빌드·검증 파이프라인을 **직접 돌려보고 수치로 남기는** 것이 목적이다.
다루는 대상은 전부 공개된 오픈소스(Bazel, Yocto, QEMU, LAVA)와 공개 문서뿐이다.

**이 저장소에 들어오지 않는 것**: 특정 조직의 내부 스택·정책·코드, 개인 경력 기록.
그런 내용은 짝이 되는 비공개 저장소에 둔다.
